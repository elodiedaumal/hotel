import React from 'react';
import HeroImg from '../Images/heroImg.jpg';

const Hero = () => {
  return (
    <section>
      <div class=''>
        <img src={HeroImg} alt='' />
      </div>
    </section>
  );
};

export default Hero;
